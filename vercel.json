{
  "version": 2,
  "buildCommand": "pnpm -w install && pnpm -C packages/frontend build",
  "installCommand": "cd .. && pnpm i -w --frozen-lockfile",
  "outputDirectory": "dist",
  "framework": "vite",
  "git": {
    "deploymentEnabled": {
      "main": true,
      "master": true
    }
  }
}
